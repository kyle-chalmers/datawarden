{
  "$comment": "FIXTURE — correctly hardened project settings: the false-positive guard for ai-config-audit. Expect zero findings from project scope.",
  "permissions": {
    "allow": ["Bash(git status)", "Bash(git diff *)"],
    "deny": ["Read(./.env)", "Read(./.env.*)", "Read(./secrets/**)"]
  }
}
